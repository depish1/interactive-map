import styled from 'styled-components';

export const Wrapper = styled.aside`
  background: ${({ theme }) => theme.colors.white1};
  border-right: ${({ theme }) => theme.colors.black4};
  width: 30rem;
`;
